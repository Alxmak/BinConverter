<Page x:Class="TweakFirmware.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:loc="clr-namespace:TweakFirmware.Services"
      xmlns:c="clr-namespace:TweakFirmware.Controls"
      Title="{loc:Loc Settings_Title}"
      ScrollViewer.CanContentScroll="False">

    <Page.Resources>
        <!-- Пункт 11: голубая рамка вокруг выбранного варианта -->
        <Style x:Key="OptionButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
    </Page.Resources>

    <ScrollViewer x:Name="RootScroll" Padding="24,16" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="960" HorizontalAlignment="Stretch">

            <!-- Карточки со значком в плитке — та же IconCard, что во вкладке
                 «О программе»: плитка и название слева, действие справа.

                 Когда-то здесь стоял CardControl из WPF-UI, и убрали его как раз за
                 такое расположение: тогда так не выглядела больше ни одна вкладка
                 программы, и «Настройки» выбивались из общего вида. Теперь так собрана
                 «О программе» — и выбивались бы уже «Настройки», оставь мы прежний вид.

                 Карточки с папками устроены иначе — заголовок сверху, строка пути под
                 ним во всю ширину; почему, сказано перед ними. -->
            <c:IconCard Symbol="PaintBrush24" Accent="#6366F1" Title="{loc:Loc Settings_ThemeTitle}">
                <c:IconCard.Action>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="{loc:Loc Settings_ThemeLight}" Command="{Binding SetThemeCommand}" CommandParameter="Light">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource OptionButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentTheme}" Value="Light">
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="{loc:Loc Settings_ThemeDark}" Command="{Binding SetThemeCommand}" CommandParameter="Dark">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource OptionButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentTheme}" Value="Dark">
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <!-- У последней кнопки ряда отступ справа снят: у OptionButton он общий,
                             а ряд теперь прижат к правому краю карточки, и лишние восемь точек
                             отодвинули бы его от края — кнопки перестали бы стоять по одной
                             линии с кнопками соседних карточек. -->
                        <Button Content="{loc:Loc Settings_ThemeSystem}" Command="{Binding SetThemeCommand}" CommandParameter="System" Margin="0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource OptionButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentTheme}" Value="System">
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </c:IconCard.Action>
            </c:IconCard>

            <c:IconCard Symbol="LocalLanguage24" Accent="#0D9488" Title="{loc:Loc Settings_LanguageTitle}">
                <c:IconCard.Action>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="{loc:Loc Settings_LanguageRu}" Command="{Binding SetLanguageCommand}" CommandParameter="ru">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource OptionButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedLanguage}" Value="ru">
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <!-- Отступ справа снят по той же причине, что у «Как в системе». -->
                        <Button Content="{loc:Loc Settings_LanguageEn}" Command="{Binding SetLanguageCommand}" CommandParameter="en" Margin="0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource OptionButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedLanguage}" Value="en">
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </c:IconCard.Action>
            </c:IconCard>

            <!-- Каждая папка — своя карточка со своим заголовком. Подписи «Папка назначения»
                 у строки нет: карточка уже названа по своей папке, а слева на её месте
                 стоит галочка — она и работает подписью строки. Колонка та же
                 (LabelColumnWidth), поэтому поле стоит там же, где на рабочих вкладках.

                 Значок у всех трёх один: это одна и та же настройка, только для разных
                 разделов, и разные картинки заставляли бы искать между ними разницу,
                 которой нет. Цвет при этом у каждой свой — иначе столбец из трёх
                 одинаковых плиток выглядит недоделанным. -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <c:CardHeader Symbol="FolderArrowRight24" Accent="#2F6FED" Title="{loc:Loc Settings_ConvertFolderTitle}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{StaticResource LabelColumnWidth}"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" Style="{StaticResource CardCheckBox}" VerticalAlignment="Center"
                                  Content="{loc:Loc Settings_UseDefaultPathCheckbox}" IsChecked="{Binding UseDefaultConvertPath}"/>
                        <ui:TextBox ClearButtonEnabled="False" Grid.Column="1" Text="{Binding ConvertFolder, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"
                                    PlaceholderText="{loc:Loc Common_NoFolderSelected}"
                                    IsEnabled="{Binding CanEditConvertPath}"
                                    AllowDrop="True" PreviewDragOver="ConvertFolderBox_DragOver" PreviewDrop="ConvertFolderBox_Drop"/>
                        <Button Grid.Column="2" Content="{loc:Loc Common_BrowseButton}" Command="{Binding BrowseConvertFolderCommand}"
                                IsEnabled="{Binding CanEditConvertPath}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <c:CardHeader Symbol="FolderArrowRight24" Accent="#16A34A" Title="{loc:Loc Settings_MergeFolderTitle}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{StaticResource LabelColumnWidth}"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" Style="{StaticResource CardCheckBox}" VerticalAlignment="Center"
                                  Content="{loc:Loc Settings_UseDefaultPathCheckbox}" IsChecked="{Binding UseDefaultMergePath}"/>
                        <ui:TextBox ClearButtonEnabled="False" Grid.Column="1" Text="{Binding MergeFolder, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"
                                    PlaceholderText="{loc:Loc Common_NoFolderSelected}"
                                    IsEnabled="{Binding CanEditMergePath}"
                                    AllowDrop="True" PreviewDragOver="MergeFolderBox_DragOver" PreviewDrop="MergeFolderBox_Drop"/>
                        <Button Grid.Column="2" Content="{loc:Loc Common_BrowseButton}" Command="{Binding BrowseMergeFolderCommand}"
                                IsEnabled="{Binding CanEditMergePath}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <c:CardHeader Symbol="FolderArrowRight24" Accent="#D97706" Title="{loc:Loc Settings_ExtractFolderTitle}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{StaticResource LabelColumnWidth}"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" Style="{StaticResource CardCheckBox}" VerticalAlignment="Center"
                                  Content="{loc:Loc Settings_UseDefaultPathCheckbox}" IsChecked="{Binding UseDefaultExtractPath}"/>
                        <ui:TextBox ClearButtonEnabled="False" Grid.Column="1" Text="{Binding ExtractFolder, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"
                                    PlaceholderText="{loc:Loc Common_NoFolderSelected}"
                                    IsEnabled="{Binding CanEditExtractPath}"
                                    AllowDrop="True" PreviewDragOver="ExtractFolderBox_DragOver" PreviewDrop="ExtractFolderBox_Drop"/>
                        <Button Grid.Column="2" Content="{loc:Loc Common_BrowseButton}" Command="{Binding BrowseExtractFolderCommand}"
                                IsEnabled="{Binding CanEditExtractPath}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Пояснение отдано карточке свойством Description, а не поставлено строкой
                 под заголовком: так оно встаёт в ту же колонку, что и название, а не под
                 плиткой со значком. -->
            <c:IconCard Symbol="DocumentBulletList24" Accent="#64748B"
                        Title="{loc:Loc Settings_ClearLogTitle}"
                        Description="{loc:Loc Settings_ClearLogDescription}">
                <c:IconCard.Action>
                    <Button Content="{loc:Loc Settings_ClearLogButton}" Command="{Binding ClearLogCommand}"
                            MinHeight="{StaticResource ActionButtonMinHeight}"/>
                </c:IconCard.Action>
            </c:IconCard>
        </StackPanel>
    </ScrollViewer>
</Page>
